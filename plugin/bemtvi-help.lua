-- Auto-loaded when the plugin is on the runtimepath (sourced from `plugin/` like a
-- neovim plugin). Registers :help / :h with defaults so help works out of the box;
-- setup() is idempotent, so a user calling require("bemtvi-help").setup() is harmless.
require("bemtvi-help").setup()
